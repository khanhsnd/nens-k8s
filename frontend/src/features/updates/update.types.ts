/** Where this copy of Nens stands against the newest published release. */
export type UpdateStatus = {
  current: string
  latest: string
  available: boolean
  page: string
  development: boolean
}
